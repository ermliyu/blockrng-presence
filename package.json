{
  "name": "blockrng-presence",
  "version": "2.0.0",
  "description": "Roll A Cube bot sidecar: keeps the Discord gateway open (online dot + join/message events forwarded to the Cloudflare Worker) and renders the PNG cards (welcome / rank / level-up / leaderboard) the Worker can't render on its CPU budget.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "preview": "node preview.js"
  },
  "engines": {
    "node": ">=22 <27"
  },
  "dependencies": {
    "@resvg/resvg-wasm": "2.6.2",
    "satori": "0.33.4"
  },
  "private": true,
  "license": "UNLICENSED"
}
